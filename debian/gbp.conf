[DEFAULT]
upstream-branch = gc-alpha
debian-branch = alpha-ubuntu-bionic
#upstream-tag = v%(version)s
upstream-tag = gc-alpha

[buildpackage]
ignore-new = true
pbuilder = true
dist = bionic
arch = amd64
